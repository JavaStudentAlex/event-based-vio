# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | Synchronization and export policy for S02 | Nearest-neighbor timestamp association within caller-provided tolerance (no interpolation in S02); diagnostics are mandatory and include counts, ranges, first/last matched ts, overlap sufficiency; CSV export preserves all rows with health labels per fixed 15-column schema; TUM export includes only valid (OK/DEGRADED) poses and records filtered counts; timestamps are UNIX seconds with 9 decimal places; quaternion order is qx,qy,qz,qw. | Locks externally-visible behavior so S03 CLI/backends and S04 evaluator can rely on deterministic association and artifact formats; avoids downstream rewrites and ensures CI can validate contract with synthetic fixtures. | Yes | agent |
| D002 |  | architecture | CLI entrypoint and run-directory naming for S03 | Provide a Python module entrypoint at `python -m nav_benchmark.run` with subcommand `run` and `--method imu_only`. Flags: `--dataset {synthetic\|mvsec}`, `--sequence <name>`, `--input <path>` (required when dataset=mvsec), `--output-root runs` (default: `runs`), `--resume` (on collision, append suffix `-r{N}`). Run directory name: `runs/<YYYYmmdd_HHMMSS>_<method>_<sequence>` with 24h timestamp. | Simple, reproducible CLI that composes loaders → backend → exporters and writes a stable artifact skeleton. The timestamped naming is deterministic, collides rarely, and the `-rN` suffix is easy to test and parse later. | Yes | agent |
| D003 |  | architecture | IMU-only health thresholds and TUM filtering policy for S03 | Health labeling for imu_only: default OK; DEGRADED when either (a) IMU sample gap dt > 0.03 s and <= 0.10 s, or (b) \|\|acc\|\| > 30 m/s^2, or (c) \|\|gyro\|\| > 10 rad/s. LOST when dt > 0.10 s, or any NaN/Inf in inputs/propagation, or orientation becomes invalid (non-finite norm). INVALID reserved for initialization failure. TUM export continues S02 rule: exclude LOST and INVALID rows; project CSV includes every row with its health label. | Deterministic, conservative thresholds that preserve rows for robustness analysis while keeping TUM compatibility. Encodes reproducible behavior across backends and aligns with R003's fixed columns and health semantics. | Yes | agent |
| D004 |  | architecture | Odometry backend interface shape for S03 | Introduce `src/nav_benchmark/baselines/base.py` defining `BaseOdometryBackend` with `run(sequence, *, config) -> Trajectory`. Provide `ImuOnlyBackend` in `src/nav_benchmark/baselines/imu.py` with a simple dataclass `ImuOnlyConfig` (gravity vector, initial pose/velocity, health thresholds). Keep interface minimal and functional to enable later backends (image_imu, event_imu, multimodal). | A tiny, explicit interface isolates the CLI orchestration from backend specifics, simplifies testing, and allows swap-in of additional backends in later milestones without touching the run path. | Yes | agent |
| D005 | M001-ncx5an/S04 | architecture | Design of S04 evaluation and plotting utilities pipeline | Implement project-native Python evaluation script and metric calculations using numpy/scipy directly where appropriate, supplemented by evo's core geometry and metrics modules. We construct evo PoseTrajectory3D using the wxyz quaternion convention and align trajectories globally using evo's Umeyama SE(3) alignment. We write out metrics.json, error_vs_time.csv, error_vs_distance.csv, trajectory_plot.png, and drift_over_distance.png. | Aligns with S04 requirements: needs global SE3 alignment, ATE, RPE, final drift, coverage, time-indexed and distance-indexed error CSVs, and visualization plots. Constructing PoseTrajectory3D using wxyz quaternions is clean and maps naturally to our trajectory representation. | Yes | agent |
| D006 | M001-ncx5an/S04 | evaluation | S04 alignment and outlier policy for drift evaluation | Use nearest-neighbor timestamp association followed by one global SE(3) alignment over overlapping OK and DEGRADED estimate poses with no robust trimming, no outlier rejection, no time-offset search, and no local/sliding realignment in M001. | This keeps synthetic and CI evaluation deterministic, matches the S04 scope boundary, preserves LOST and INVALID intervals in coverage accounting instead of hiding them, and records the policy explicitly in metrics.json for future comparison changes. | Yes, future milestones may add separately reported robust or time-offset variants without changing the M001 baseline contract. | agent |
| D007 | M001-ncx5an/S04 | evaluation | S04 error series artifact schema | Write error_vs_time.csv with timestamp, estimated_xyz, aligned_ground_truth_xyz, xyz error, error magnitude, health, and association residual columns; write error_vs_distance.csv with cumulative distance, error magnitude, health, association residual, and 20 m bin fields used by the drift plot. | A stable series schema lets S05 validate artifact contents and lets plots be regenerated from the same numeric series rather than placeholders while keeping invalid health states visible but excluded from numeric metric aggregation. | Yes, additive columns are allowed later if readers preserve the core fields. | agent |
| D008 | M001-ncx5an/S06 | milestone | Override response for M001-ncx5an/S06 completion | Execute final tasks of Slice 6 and perform the sequence to complete the slice and milestone M001 directly within the remaining tasks of S06. | The user provided an explicit override: "please help me to fix the last ations with the slice 6, implement it and finish it completely same as finish the whole milestone". Therefore, the plans have been updated to ensure T04 leads directly to slice and milestone completion. | Yes | human |
| D009 | M002 | architecture | How to address the camera equals body frame simplification in event_imu backend | Extract IMU-to-camera rotation from Calibration.data when imu_cam_transform_available is True; fall back to identity rotation when absent. Apply the inverse rotation in _event_world_displacement before the world-body rotation. | MVSEC provides IMU-to-camera extrinsics in calibration data. Using these transforms makes the event shift to world displacement conversion geometrically correct. The fallback preserves backward compatibility when calibration is absent. | Yes — may be superseded if a more sophisticated extrinsics model (translation + rotation) is needed | agent |
| D010 | M002 | architecture | M002 scope and ordering — what work remains given existing backends | Three slices: S01 hardening extrinsics (high risk), S02 cross-method schema validation (medium risk), S03 synthetic benchmark comparison (low risk). All validation is synthetic — no MVSEC download required in CI. | The event_imu, image_imu, and multimodal_vio backends already exist with tests and CLI integration from M001. M002 focuses on correctness hardening (extrinsics), formal schema validation (R013), and benchmark comparison proof (R012). Risk-first ordering puts the math change first. | Yes | agent |
