# S01: Extrinsics-aware Event+IMU Correction

**Goal:** Modify the event_imu backend to extract and apply IMU-to-camera extrinsics from the MVSEC calibration data, making the event shift to world displacement conversion geometrically correct instead of assuming camera equals body frame
**Demo:** After this: EventImuBackend uses calibrated IMU-to-camera rotation from MVSEC calibration data to transform event-derived displacements into the body frame; synthetic tests prove extrinsics path works correctly and existing tests still pass

## Must-Haves

- Complete the planned slice outcomes.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: Wire extrinsics rotation into event_imu correction pipeline
  - Files: `src/nav_benchmark/baselines/event_imu.py`, `src/nav_benchmark/datasets/mvsec.py`
  - Verify: uv run --only-dev ruff check src/nav_benchmark/baselines/event_imu.py
- [x] **T02**: Test extrinsics-aware correction and verify no regressions
  - Files: `tests/baselines/test_event_imu_backend.py`
  - Verify: uv run pytest tests/baselines/test_event_imu_backend.py -q --tb=short
</tasks>

## Files Likely Touched

- src/nav_benchmark/baselines/event_imu.py
- src/nav_benchmark/datasets/mvsec.py
- tests/baselines/test_event_imu_backend.py
