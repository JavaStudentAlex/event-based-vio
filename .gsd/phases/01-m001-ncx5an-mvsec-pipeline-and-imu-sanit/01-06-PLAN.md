# S06: Remediate validation string mismatch and run validation verification

**Goal:** Fix validation string mismatch and add regression test locking canonical artifact strings, proving the full run->eval->validate pipeline passes without status/alignment mismatches.
**Demo:** The validation command successfully validates runs without throwing a string mismatch error, and the validate subcommand passes successfully.

## Must-Haves

- `test_validate_locks_canonical_artifact_strings` passes, asserting exact canonical values for manifest status, alignment policy, evaluation status, and metrics status
- Full `run -> eval -> validate` CLI proof passes with exit code 0 and 11/11 checks
- All 268 tests pass, ruff check clean, ruff format clean

## Proof Level

- This slice proves: automated-test + CLI proof

## Integration Closure

Validation module accepts all artifacts produced by run and eval commands without string mismatch

## Verification

- None - no new observability surfaces

<tasks>
- [x] **T01**: Regression test locking canonical artifact strings already exists and passes _(15min)_
  Add test_validate_locks_canonical_artifact_strings to tests/cli/test_validate_cli.py that runs synthetic run->eval->validate and asserts exact canonical values: manifest.status=success, manifest.alignment.policy=nearest_neighbor, manifest.evaluation.status=success, metrics.status=OK, metrics.config.alignment_policy=se3, failure_notes contains clean-run sentence, and validate exits 0 with 11/11 passes.
  - Files: `tests/cli/test_validate_cli.py`
  - Verify: uv run --only-dev pytest tests/cli/test_validate_cli.py::test_validate_locks_canonical_artifact_strings -q
- [x] **T02**: Full verification passes: 268 tests, clean lint, clean format _(5min)_
  Run ruff check, ruff format --check, and pytest tests -q. Confirm 268+ tests pass with zero failures.
  - Verify: uv run --only-dev ruff check . && uv run --only-dev ruff format --check . && uv run pytest tests -q
</tasks>

## Files Likely Touched

- tests/cli/test_validate_cli.py
