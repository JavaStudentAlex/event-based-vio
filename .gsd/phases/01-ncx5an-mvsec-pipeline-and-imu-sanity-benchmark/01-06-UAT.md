# S06: Remediate validation string mismatch and run validation verification — UAT

**Milestone:** M001-ncx5an
**Written:** 2026-07-05T09:44:56.717Z

## Preconditions
1. UV is installed and the `uv run` environment is configured.
2. A synthetic sequence generator is available in `src/cli/test_validate_cli.py`.

## Steps
1. Generate synthetic input: `PYTHONPATH=src:tests uv run python -c "from pathlib import Path; from cli.test_validate_cli import _write_non_coplanar_sequence; _write_non_coplanar_sequence(Path('/tmp/s06_input'))"`
2. Run baseline: `PYTHONPATH=src uv run python -m nav_benchmark run --method imu_only --dataset synthetic --sequence test --input /tmp/s06_input --output-root /tmp/s06_verify`
3. Identify run dir: `RUN_DIR=$(ls -td /tmp/s06_verify/*/ | head -n1)`
4. Evaluate run: `PYTHONPATH=src uv run python -m nav_benchmark eval --run-dir "$RUN_DIR"`
5. Validate artifacts: `PYTHONPATH=src uv run python -m nav_benchmark validate --run-dir "$RUN_DIR"`
6. Verify tests and linting: `uv run --only-dev pytest tests/ -q && uv run --only-dev ruff check . && uv run --only-dev ruff format --check .`

## Expected
- The validate command (`step 5`) completes with exit code 0 and reports "Validation: 11/11 checks passed."
- No string mismatch error is raised for `failure_notes.md`.
- Tests and linters pass cleanly.

## Edge Cases
- Degraded data sequences should still write `failure_notes.md` with explicit error boundaries, and this behavior remains strict under the validation fix.

## UAT Type
- UAT mode: runtime-executable
