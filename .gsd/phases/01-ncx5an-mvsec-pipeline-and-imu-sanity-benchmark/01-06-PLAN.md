# S06: Remediate validation string mismatch and run validation verification

**Goal:** Fix the validation string mismatch so the validate subcommand succeeds on a synthetic M001 run, closing the M001 benchmark foundation.
**Demo:** The validation command successfully validates runs without throwing a string mismatch error, and the validate subcommand passes successfully.

## Must-Haves

- 1. The string produced by run.py _intervals_text() matches what validation.py expects.
- 2. All existing tests pass (pytest tests -q).
- 3. Lint and format checks pass (ruff check . && ruff format --check .).
- 4. A synthetic run → eval → validate CLI path exits 0.

## Proof Level

- This slice proves: runtime — synthetic run→eval→validate pipeline proves the fix end-to-end

## Integration Closure

Consumes S05 validation module and CLI validate subcommand. Produces a passing validate exit code, closing the M001 artifact contract loop.

## Verification

- validate subcommand now exits 0 on valid runs instead of erroring on the string mismatch

<tasks>
- [x] **T01**: Aligned the failure note text output in run.py to match the exact string expected by validation.py. _(XS (single string edit + verification))_
  ## Why
  `run.py` line 88 produces `"No degraded or lost intervals were detected during this run."` but `validation.py` line 326 expects `"No degraded or lost intervals were detected."`. This causes the validate subcommand to fail on otherwise valid runs.
  - Files: `src/nav_benchmark/run.py`
  - Verify: uv run --only-dev ruff check . && uv run --only-dev ruff format --check . && uv run pytest tests -q
- [x] **T02**: Completed synthetic end-to-end run-eval-validate verification pipeline _(S (run three CLI commands, verify exit codes))_
  ## Why
  The final proof for S06 and M001 closure is a synthetic end-to-end CLI path: `run → eval → validate` must exit 0 without requiring MVSEC data.
  - Files: `src/nav_benchmark/run.py`, `src/nav_benchmark/validation.py`
  - Verify: uv run python -m nav_benchmark run --method imu_only --dataset synthetic --sequence test --output-dir /tmp/s06_verify && uv run python -m nav_benchmark eval --run-dir /tmp/s06_verify && uv run python -m nav_benchmark validate --run-dir /tmp/s06_verify
</tasks>

## Files Likely Touched

- src/nav_benchmark/run.py
- src/nav_benchmark/validation.py
