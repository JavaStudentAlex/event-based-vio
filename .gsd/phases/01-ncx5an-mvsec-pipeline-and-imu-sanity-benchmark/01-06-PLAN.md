# S06: Remediate validation string mismatch and run validation verification

**Goal:** Verify the validation string mismatch is remediated, add targeted regression coverage for the canonical failure_notes string, fix the remaining lint issue, and confirm the full synthetic run → eval → validate path passes end-to-end.
**Demo:** The validation command successfully validates runs without throwing a string mismatch error, and the validate subcommand passes successfully.

## Must-Haves

- 1. `rtk uv run --only-dev pytest tests/ -q` passes all tests including any new regression test for the canonical failure_notes string.\n2. `rtk uv run --only-dev ruff check .` exits 0 (no lint errors).\n3. `rtk uv run --only-dev ruff format --check .` exits 0.\n4. The synthetic `run → eval → validate` CLI path exits 0 and reports all checks passed.\n5. Validation remains strict: tests that corrupt the canonical string or artifact content still fail validation (nonzero exit).

## Proof Level

- This slice proves: CI-synthetic: all verification runs against synthetic data without MVSEC downloads

## Integration Closure

S06 consumes the validation module (validation.py), CLI wiring (run.py), and artifact producers from S03-S05. It produces a verified, regression-tested validation path that M002 can reuse for event_imu outputs.

## Verification

- No new observability surfaces; validate subcommand exit code and concise report remain the primary signal.

<tasks>
- [x] **T01**: Aligned the failure note text output in run.py to match the exact string expected by validation.py. _(XS (single string edit + verification))_
  ## Why
  `run.py` line 88 produces `"No degraded or lost intervals were detected during this run."` but `validation.py` line 326 expects `"No degraded or lost intervals were detected."`. This causes the validate subcommand to fail on otherwise valid runs.
  - Files: `src/nav_benchmark/run.py`
  - Verify: uv run --only-dev ruff check . && uv run --only-dev ruff format --check . && uv run pytest tests -q
- [x] **T02**: Completed synthetic end-to-end run-eval-validate verification pipeline _(S (run three CLI commands, verify exit codes))_
  ## Why
  The final proof for S06 and M001 closure is a synthetic end-to-end CLI path: `run → eval → validate` must exit 0 without requiring MVSEC data.
  - Files: `src/nav_benchmark/run.py`, `src/nav_benchmark/validation.py`
  - Verify: rm -rf /tmp/s06_input /tmp/s06_verify && PYTHONPATH=src:tests uv run python -c "from pathlib import Path; from cli.test_validate_cli import _write_non_coplanar_sequence; _write_non_coplanar_sequence(Path('/tmp/s06_input'))" && PYTHONPATH=src uv run python -m nav_benchmark run --method imu_only --dataset synthetic --sequence test --input /tmp/s06_input --output-root /tmp/s06_verify && RUN_DIR=$(ls -td /tmp/s06_verify/*/ | head -n1) && PYTHONPATH=src uv run python -m nav_benchmark eval --run-dir "$RUN_DIR" && PYTHONPATH=src uv run python -m nav_benchmark validate --run-dir "$RUN_DIR"
- [x] **T03**: Added regression test for failure notes producer-validator contract and resolved import sorting in record_google_earth_sequence.py _(20 minutes)_
  ## Why
  The validation string mismatch between producer (`run.py`) and validator (`validation.py`) for `failure_notes.md` was fixed in commit 22746d9. The producer now writes `"No degraded or lost intervals were detected."` and the validator expects exactly that string. However, there is no dedicated regression test that specifically guards this canonical string pairing — existing tests exercise the broad validation path but do not assert on the exact producer ↔ validator string contract. Additionally, `tools/record_google_earth_sequence.py` has an unsorted import block (ruff I001).
  - Files: `tests/validation/test_artifact_validation.py`, `tools/record_google_earth_sequence.py`
  - Verify: rtk uv run --only-dev ruff check . && rtk uv run --only-dev ruff format --check . && rtk uv run --only-dev pytest tests/validation/test_artifact_validation.py -q
- [x] **T04**: Produced fresh full-suite, lint, format, and synthetic run-eval-validate proof for the S06 artifact contract. _(10 minutes)_
  ## Why
  The full M001 artifact contract must be proven end-to-end: a synthetic `run → eval → validate` pipeline must exit 0 with all checks passed, and the complete test suite must be green. This is the closure proof for M001 — it demonstrates that all artifacts produced by the benchmark pipeline are accepted by the validation module without string mismatches or artifact contract violations.
  - Verify: rtk uv run --only-dev pytest tests/ -q && rtk uv run --only-dev ruff check . && rtk uv run --only-dev ruff format --check .
</tasks>

## Files Likely Touched

- src/nav_benchmark/run.py
- src/nav_benchmark/validation.py
- tests/validation/test_artifact_validation.py
- tools/record_google_earth_sequence.py
